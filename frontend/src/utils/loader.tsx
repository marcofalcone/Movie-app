import React from 'react'
import ClipLoader from 'react-spinners/ClipLoader'

export const loader = (
  <ClipLoader
    color={'#11bfe5'}
    size={50}
    aria-label="Loading Spinner"
    data-testid="loader"
  />
)
